
# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
# Sensitive credentials
.env

# Playwright storage state (Authentication tokens)
playwright/.auth/
auth.json

# Local test outputs (Screenshots and reports)
screenshots/
test-results/
playwright-report/
blob-report/
# Generated each run by custom-reporter.ts (large, embeds base64 screenshots)
custom-report.html
# Machine-readable run summary emitted alongside the report (for the emailer)
report-summary.json
